Assumptions - 

1. We assume that single user will have a single role.
2. This is due to clashing workbenches. If a single user has multiple roles, then access to workbenches will collide which we are not handling.